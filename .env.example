# Table requests are emailed with Resend (https://resend.com).
# Without all three set, the form refuses to pretend it sent and tells the
# visitor to phone the restaurant instead.
RESEND_API_KEY=
RESERVATION_EMAIL_TO=bookings@example.com
RESERVATION_EMAIL_FROM="Cambodian Food Stars <bookings@yourdomain.com>"
